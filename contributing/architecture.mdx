---
title: "Site architecture"
description: "How the IPG documentation site is organised — repo layout, navigation tree, and the 8 document types."
icon: "sitemap"
---

<Note>
**Placeholder.** Phase 6 of the IPG docs rollout. Source of truth: `.claude/ARCHITECTURE.md` and `.claude/DOC_TYPES.md` (both gitignored — this page distils them for public contributors).
</Note>

## What this page will cover

- Repo layout (top-level directories and what each contains)
- The 9-tab navigation tree from `docs.json` and the rationale for each tab
- The 8 document types: landing, conceptual, how-to, reference, pipeline-stage, troubleshoot, changelog, cookbook
- The component-to-doc-type matrix (which Mintlify components are allowed where)
- The `docType:` + `audience:` frontmatter contract
- The "Cookbook tab" pattern (preserved Mintlify starter examples as a living component reference)
- Cross-repo dependencies on the sister pipeline `../ipg`
