---
title: "Changelog"
description: "Release notes for the IPG pipeline and this documentation site."
icon: "list"
---

<Note>
**Placeholder.** Phase 6 of the IPG docs rollout. The real page will use Mintlify's `<Update>` component, one entry per release.
</Note>

## Roadmap

- **`ipg` v1.0.0** — first tagged release of the Nextflow port (steps 1–31). Pending pipeline Day 3+4 testing and lint.
- **`ipg-docs` v1.0.0** — first production deployment of this documentation site.
- **`ipg-origins` v0.1.0** — sister pipeline scaffolding for the unported tools (`filter_FPKM`, `origins`, `msDot`, `db_compare`). See [Related work](/background/related-work).

## Past releases

(Empty until v1.0.0 is tagged.)
