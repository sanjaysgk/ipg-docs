---
title: "Run locally with Docker"
description: "Laptop / workstation dry-run on the chr22 test sample."
icon: "container"
---

<Note>
**Placeholder.** Phase 6 of the IPG docs rollout.
</Note>

## What this page will cover

- Docker Desktop install pointer (Linux / macOS / Windows WSL2)
- Required disk and RAM (~32 GB recommended for STAR even on chr22)
- `nextflow run sanjaysgk/ipg -profile test,docker`
- Inspecting `.nextflow.log` and the `work/` directory for debugging
- Why Conda is a fallback, not the default
